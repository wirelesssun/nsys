{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Sort Project", // 记住这个标签名
            "type": "shell",
            "command": "mkdir -p build && cd build && cmake .. && make -j$(nproc)",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}