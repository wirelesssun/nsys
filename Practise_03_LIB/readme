构建与运行步骤（以 Linux 为例）

由于您在 Company 的开发环境可能涉及多个服务器或本地环境，建议通过以下步骤验证：

第一步：编译并安装工程 A
Bash
cd MathLibrary
mkdir build && cd build
# 设置安装目录为本地的 'dist' 文件夹，避免污染系统目录
cmake .. -DCMAKE_INSTALL_PREFIX=../dist
make install

第二步：编译工程 B 并调用
Bash
cd ../../AppProject
mkdir build && cd build
# 告诉 CMake 到工程 A 的安装目录里找配置文件
cmake .. -DTestAdd_DIR=../MathLibrary/dist/lib/cmake/TestAdd
make
./MyApp